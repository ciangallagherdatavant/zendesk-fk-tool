# FK Analysis Result

**Article:** Create a Single Configuration
**Date:** 01 April 2026 15:21
**Calculated by:** Python textstat library

---
FLESCH-KINCAID GRADE LEVEL ANALYSIS

Article Title: Create a Single Configuration

SCORE: 11.8
Reading Level: High School Advanced
Target Audience: Readers with advanced high school education or some college experience

Summary: This article requires a high school advanced reading level and is approaching the upper limit for technical documentation but still meets the Grade 12 or below target.

FK RECOMMENDATIONS:
✓ Content meets target (Grade 12 or below) - Well done!

This article scores 11.8, which falls just within the acceptable range for technical documentation. To improve accessibility further and move toward the ideal Grade 7-8 range, consider:

1. **Break down complex compound sentences** - Example: "A configuration is a mapping of rules to fields contained within a data table and is used for tokenization and data onboarding to the Datavant Connect Platform" could become: "A configuration maps rules to fields in a data table. You use it for tokenization and data onboarding to the Datavant Connect Platform."

2. **Simplify technical phrases** - Replace "enumerating column numbers along with each column's content" with "listing column numbers and their content."

3. **Reduce multi-clause instructions** - Example: "After creating a new configuration, add a unique name to the configuration and select the site name to associate it with" could be split: "After creating a new configuration, add a unique name. Then select the site name to associate it with."

WCAG 2.2 WRITING ACCESSIBILITY NOTES:

**Plain Language:** Needs Attention - Some phrases are unnecessarily complex: "enumerating column numbers," "mapped column positions must match," "auto-create tokens when you create a configuration for the first time."

**Sentence Length:** Pass - Most sentences are under 25 words. Longest identified: "A configuration is a mapping of rules to fields contained within a data table and is used for tokenization and data onboarding to the Datavant Connect Platform" (28 words) - consider breaking this one down.

**Jargon:** Needs Attention - Several technical terms lack explanation or context:
- "tokenization" (used before explanation)
- "de-identify data" 
- "PII" (unexplained abbreviation)
- "tokenization engine"
- "Data Type" vs "data type" (inconsistent capitalization suggests specific meaning)

**Active Voice:** Needs Attention - Multiple passive constructions found:
- "is used for tokenization"
- "are present in the input file"
- "should be mapped"
- "must be updated"
- "must match"

**Heading Clarity:** Pass - Headings are clear and follow a logical step-by-step structure (Step 1, Step 2, etc.)

**Link Text:** Pass - Links appear contextual ("What is a Configuration user guide," "Creating Bulk Configurations user guide")

**Abbreviations:** Needs Attention
- "PII" - not explained (should be "personally identifiable information (PII)" on first use)
- Inconsistent use of "e.g." vs "etc" vs "i.e." formatting

Overall WCAG Writing Score: Needs Attention