# FK Analysis Result

**Article:** Datavant CLI Commands and Optional Arguments
**Date:** 01 April 2026 15:06
**Calculated by:** Python textstat library

---
FLESCH-KINCAID GRADE LEVEL ANALYSIS

Article Title: Datavant CLI Commands and Optional Arguments

SCORE: 12.8
Reading Level: High School Advanced / Early College
Target Audience: Readers with advanced high school or college-level reading ability, comfortable with technical documentation

Summary: This article requires a reading ability slightly above the ideal target for technical documentation and may challenge some users in your intended audience.

FK RECOMMENDATIONS:

While the score of 12.8 is close to the Grade 12 target, it exceeds the recommended maximum for technical documentation. Here are three specific suggestions to lower complexity:

1. **Break down the opening paragraph**: The first paragraph contains multiple complex clauses. Split it into shorter, clearer sentences:
   - Current: "This article includes information that relates to Datavant CLI v5, which is available on limited release."
   - Suggested: "This article covers Datavant CLI v5. CLI v5 is available on limited release."

2. **Simplify technical explanations**: The sentence "When tokenizing with the CLI, tokenize and transform must be run as separate commands" uses a participial phrase that adds complexity.
   - Suggested: "The CLI requires separate commands. You must run tokenize and transform separately."

3. **Reduce multi-syllable word density**: Replace complex words where simpler alternatives exist:
   - "implement" → "use"
   - "generates" → "creates"
   - "additional" → "more"

FK WCAG 2.2 WRITING ACCESSIBILITY NOTES:

**Plain Language:** Needs Attention - Some sentences use unnecessarily complex constructions. Example: "If you're tokenizing using the Desktop Application, you may select tokenizing identified data and transforming tokens to send to a partner, which generates both a tokenized and transformed output in a single workflow" contains nested technical concepts that could be simplified.

**Sentence Length:** Pass - Average 9.8 words per sentence is excellent and well below the 25-word guideline.

**Jargon:** Needs Attention - Several unexplained technical terms appear:
- "batch mode," "server mode," "streaming mode" (mentioned but not defined)
- "tokenize" and "transform" (used extensively without initial definition)
- "cat" and "type" (command-line terms not explained)
- "PowerShell" and "cmd" (not defined for non-Windows users)

**Active Voice:** Pass - Most instructions use active voice appropriately (e.g., "Connect with your Datavant contact").

**Heading Clarity:** Pass - Headings clearly describe the content that follows, using consistent structure like "Windows, tokenize" and "Mac, transform-tokens to."

**Link Text:** Pass - Link references are descriptive (e.g., "Use Datavant CLI in Server Mode" and "Use Datavant via Cloud Provider Native Applications").

**Abbreviations:** Needs Attention - Several abbreviations lack first-use definitions:
- "CLI" (used extensively; appears defined only in title)
- "OS" (used once without expansion)
- "v5" (version abbreviation)

**Overall WCAG Writing Score:** Needs Attention

The article has excellent sentence length and structure, but needs work on defining technical terms, expanding abbreviations on first use, and simplifying some complex explanations to meet accessibility standards.

---